# Production Supabase — committed to git, safe to share.
# The anon key here is the PUBLISHABLE key — not sensitive.
# Override per-mode settings in .env.local (gitignored).

VITE_SUPABASE_URL=https://nbewyeoiizlsfmbqoist.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_jQHCvoVBeWqMuc_P40HOJg_KggDk9dh

# ── VITE_DEV_AUTH ────────────────────────────────────────────────────────────
# Controls auth mode. Must be set explicitly in .env.local (not here).
#   0 = local Supabase Docker
#   1 = prototype mode (localStorage, no auth)
#   unset = production Supabase (these values above)
